int counter;

// Start is called before the first frame update
event void Start()
{
    counter = 1000;
}

// Update is called once per frame
event void Update()
{
    counter = System::Int32.op_Addition(counter, 1);

    UnityEngine::Debug.Log((object)counter);
}